<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting"
        tools:targetApi="31">
        <activity
            android:name=".AddIngredientActivity"
            android:exported="false"
            android:label="Add Ingredient"
            android:theme="@style/Theme.AppPrototype1.NoActionBar"/>
        <activity
            android:name=".IngredientScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_ingredient"
            android:theme="@style/Theme.AppPrototype1.NoActionBar" />
        <activity
            android:name=".CocktailScreenActivity"
            android:exported="false"
            android:label="@string/cocktail"
            android:theme="@style/Theme.AppPrototype1.NoActionBar" />
        <activity
            android:name=".AddCocktailActivity"
            android:exported="false"
            android:label="Add Cocktail"
            android:theme="@style/Theme.AppPrototype1.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>